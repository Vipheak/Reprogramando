var base = 5;
var altura = 4;
var area = 0;

area = (base*altura)/2;

console.log(area);

//str es lo que hay que poner en python si es que queremos ahorrar variables como serie el caso de area en donde ponemoes printf(str(base*altura)/2);
